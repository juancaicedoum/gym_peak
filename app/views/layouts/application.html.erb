<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Peak Box Landing" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Si no estás usando PWA, puedes dejar estas líneas comentadas o eliminarlas %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Carga directamente tu archivo de estilos de la landing page %>
    <%= stylesheet_link_tag "landing_page", "data-turbo-track": "reload" %>

  </head>

  <body>
    <%= yield %>
  </body>
</html>